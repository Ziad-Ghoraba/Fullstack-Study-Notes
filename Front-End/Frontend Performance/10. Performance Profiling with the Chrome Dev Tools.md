The record button in the performance tab means that the browser will **start capturing everything the page is doing** so you can analyze performance problems.

When opening the main thread you can see the functions that was in the main thread and if you clicked some function you can see the details of the timing so you can figure out the problem that made this task long is a rendering issue or a scripting issue.